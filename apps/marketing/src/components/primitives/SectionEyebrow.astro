---
interface Props {
  index: string
  label: string
}

const { index, label } = Astro.props
---

<div class="flex items-center gap-3 text-xs font-mono tracking-[0.18em] text-text-muted">
  <span>{index}</span>
  <span class="h-px w-5 bg-border-default"></span>
  <span>{label}</span>
</div>
